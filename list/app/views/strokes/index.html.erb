<h1>LIST</h1>

<%= form_for Stroke.new do |f| %>
	<%= f.label :tags %>
	<%= f.text_field :tags %>
	<%= f.label :content %>
	<%= f.text_area :content %>
	<%= f.submit %>
<% end %>

<% @strokes.each do |stroke| %>
	<p>
		<b> <%= stroke.content %> </b> <br>
		<%= stroke.tags %>
	</p>
<% end %>